# Project-owned named tools (see sase docs/tool.md, "Guarded recipes").
# Complete entries: other config layers cannot alter this catalog.
tools:
  check:
    argv: [just, check]
    description: Run the repository scoped check.
    stages: none
    inputs:
      - Justfile
      - pyproject.toml
    args: deny
    fingerprint:
      toolchain:
        python: [.venv/bin/python, --version]
        just: [just, --version]
        sase-research-artifacts:
          [
            .venv/bin/python,
            -c,
            "from importlib.metadata import version; print(version('sase-research-artifacts'))",
          ]
